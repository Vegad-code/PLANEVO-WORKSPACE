# CLAUDE.md — Planevo

The steering for this repo lives in **`AGENTS.md`** at the root — read it first and treat
it as authoritative. It's the single source of truth shared across Codex, Cursor, and
Claude Code, so it doesn't drift.

This file exists only so Claude Code's native config points to the right place. Everything
you need — the design-phase context, the read-first docs, the inviolable rules — is in
`AGENTS.md`. Do not restate or fork those rules here.
