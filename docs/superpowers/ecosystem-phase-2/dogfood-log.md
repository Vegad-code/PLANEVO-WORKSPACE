# Phase 2 Tasks product — founder dogfood log

Gate: use `/tasks` as the primary task surface for **three consecutive weekdays**
before declaring Phase 2 complete. Each row must include a dated founder sign-off.

## Checklist (complete every dogfood day)

- [ ] Four board lanes render: To do, In progress, In review, Done
- [ ] Create task with all modal fields (title, description, priority, due, estimate, tags, attachments)
- [ ] Drag within and across lanes (including In review)
- [ ] List and table views show the same active tasks as the board
- [ ] Task Peek edits persist (including tags and estimate)
- [ ] Schedule, Attach file, Add to workspace cross-links
- [ ] Quick capture creates a product task (no kernel database)
- [ ] Workspace scope filter works

## Sign-off rows

| Date (ISO) | Weekday | Notes / issues | Founder sign-off |
|---|---|---|---|
| | | | |
| | | | |
| | | | |

## Rules

1. Do not count a day until migration gate `20260718160000` is verified (see `verification.md`).
2. Consecutive weekdays only — skip weekends, do not skip an intervening Mon–Fri without restarting the count.
3. Any blocking regression pauses the gate until fixed and reverified.
